# Обзор программы

`python main.py` открывает API-меню. Агентские команды `agent-plan` и
`agent-run-api` работают только с форматом лицо/оборот и референсами.

В программе нет браузерной автоматизации, горячих клавиш или настройки
координат.
