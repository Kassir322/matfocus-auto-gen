# Проверка version2: общий трекер

Этот файл нужен как единая точка фиксации проверки между планом `version2` и фактической реализацией в репозитории.

## Цель проверки

Сверить:

1. Что было запланировано в документах `version2`.
2. Что фактически реализовано в текущем коде репозитория.
3. Какие пункты реализованы частично, отличаются от плана или требуют отдельной ручной проверки.

## Основные документы-источники

- `version2/planning/PLAN_V2.md`
- `version2/planning/ROADMAP_V2.md`
- `version2/planning/V1_TO_V2.md`
- `version2/planning/PROJECT_STRUCTURE.md`
- `version2/reference/INTERFACE_console_menu.md`
- `version2/reference/INTERFACE_aistudio_modes.md`
- `version2/reference/INTERFACE_aistudio_helpers.md`
- `version2/reference/SETTINGS_V2.md`
- `version2/reference/HOTKEYS_V2.md`
- `version2/reference/PROCESS_MODEL.md`

## Предварительное наблюдение по состоянию репозитория

- Внутри папки `version2/` сейчас лежит только документация и `test_folder`.
- Фактическая реализация v2 находится вне `version2/`, в корне репозитория:
  - `main.py`
  - `ui/`
  - `utils/`
  - `sites/aistudio/`
  - `old_code/`
- Значит, папка `version2/` сейчас выступает как слой планирования/spec-документации, а не как папка с кодом.

## Предварительное сопоставление: план vs факт

| Блок                   | По документам                                                             | Что уже найдено в коде                                                                        | Статус проверки             | Комментарий                                                                                  |
| ---------------------- | ------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------- | --------------------------- | -------------------------------------------------------------------------------------------- |
| Отдельная структура v2 | Новый проект с `main.py`, `ui`, `utils`, `sites`, `old_code`, `data`      | В корне репозитория есть `main.py`, `ui`, `utils`, `sites`, `old_code`                        | Частично подтверждено       | Нужно отдельно проверить, насколько фактическая структура совпадает с `PROJECT_STRUCTURE.md` |
| Хранилище настроек     | `settings_store.py` с функциями загрузки/сохранения/defaults              | Найден `utils/settings_store.py`                                                              | Требует детальной сверки    | Нужно сверить состав ключей и поведение с `SETTINGS_V2.md`                                   |
| Хранилище координат    | `coordinates_store.py`, два словаря: `coordinates` и `relative_movements` | Найден `utils/coordinates_store.py`                                                           | Требует детальной сверки    | Нужно проверить JSON-структуру и UX настройки координат                                      |
| Процессы и Esc         | Жесткий stop через `terminate()`                                          | Найден `utils/process_control.py`, вызовы `start_worker`/`stop_worker`, Esc в `ui/hotkeys.py` | Предварительно подтверждено | Нужно проверить, нет ли остатков старой мягкой остановки                                     |
| Консольное меню        | Выбор сайта, режима, файла, показ плана, запуск генерации                 | Найден `ui/console_menu.py`                                                                   | Предварительно подтверждено | Нужно сверить пункты меню и сценарии из `INTERFACE_console_menu.md`                          |
| Хоткеи                 | Полный набор по `HOTKEYS_V2.md`                                           | Найден `ui/hotkeys.py`                                                                        | Требует детальной сверки    | Нужно пройтись по каждому хоткею и отличиям от документа                                     |
| AI Studio helpers      | Общие UI-функции в `sites/aistudio/helpers.py`                            | Найден `sites/aistudio/helpers.py`                                                            | Предварительно подтверждено | Нужно проверить состав helper-функций и отсутствие лишней логики в режимах                   |
| Standard режим         | `mode_standard.py`                                                        | Найден `sites/aistudio/mode_standard.py`                                                      | Предварительно подтверждено | Нужно сверить парсер, план, run_mode и правила именования                                    |
| Multiformat режим      | `mode_multiformat.py`                                                     | Найден `sites/aistudio/mode_multiformat.py`                                                   | Предварительно подтверждено | Нужно сверить порядок лицо/оборот и aspect ratio                                             |
| Режим с референсами    | `mode_multiformat_with_refs.py`                                           | Найден `sites/aistudio/mode_multiformat_with_refs.py`                                         | Предварительно подтверждено | Нужно проверить поиск референсов и шаг вставки изображения                                   |
| API-режимы             | В docs есть отдельный слой API-интеграции                                 | Найдены `*_api.py` модули                                                                     | Отдельная ветка проверки    | Нужно решить, считать ли API-ветку частью основной сверки v2 или отдельным расширением       |

## Что проверить по этапам ROADMAP_V2

### Этап 0. Базовая структура проекта

- [x] Сверить фактическую структуру репозитория с `PROJECT_STRUCTURE.md`
- [x] Проверить, что `old_code/` действительно используется как reference, а не активная логика
- [x] Проверить, есть ли все важные директории/файлы из плана

Статус этапа: `Частично совпадает`

Что подтверждено:

- В корне есть ключевые блоки v2: `main.py`, `sites/`, `utils/`, `old_code/`, `requirements.txt`, `README.md`.
- Папка `old_code/` действительно содержит копию v1: `main.py`, `config/`, `core/`, `ui/`, `utils/`, `tests/`.
- Активная логика запуска идёт не из `old_code/`, а из корневого `main.py`.

Что отличается от `PROJECT_STRUCTURE.md`:

- В документе указана папка `console/`, а в коде используется `ui/`.
- В документе указаны `utils/settings.py` и `utils/coordinates.py`, а фактически используются `utils/settings_store.py` и `utils/coordinates_store.py`.
- В реальном проекте появились дополнительные модули и подсистемы, которых нет в структуре документа:
  - `utils/generation_runner.py`
  - `utils/process_control.py`
  - `utils/api_client.py`
  - `utils/log_writer.py`
  - `utils/prompt_parsers.py`
  - API-режимы `sites/aistudio/*_api.py`
- Документ описывает `docs/` как место основной документации, но спецификация v2 фактически лежит в `version2/`.

Вывод по этапу 0:

- `old_code/` выполняет роль reference-слоя v1, как и было задумано.
- `PROJECT_STRUCTURE.md` частично устарел и уже не отражает актуальное дерево v2.
- Для дальнейшей сверки нужно считать источником истины фактический код, а `PROJECT_STRUCTURE.md` использовать как исторический архитектурный ориентир.

Отчёт о проделанной работе по этапу 0:

- В `ui/console.py` убран legacy-смешанный слой:
  - удалён `show_current_settings(...)`;
  - убраны зависимости от `config.coordinates` и старого `core.file_handler`;
  - модуль оставлен как тонкая v2-обёртка только для вывода актуальной инструкции по hotkeys.
- `utils/process_manager.py` переведён в явный legacy-compat shim:
  - добавлена прямая пометка, что это не активный runtime v2;
  - сохранён только как слой совместимости для старого API и legacy-тестов;
  - основной runtime по-прежнему должен идти через `main.py` + `utils.process_control.py` + `utils.generation_runner.py`.
- Из `utils/__init__.py` убран экспорт `ProcessManager`, чтобы пакет `utils` больше не рекламировал legacy-процессную модель как актуальную часть v2-контракта.
- Тестовый слой частично разведен:
  - в `tests/test_suite.py` `ProcessManager` переименован в `LegacyProcessManager`;
  - добавлена явная пометка, что этот набор в основном проверяет legacy-совместимость;
  - добавлен отдельный `tests/test_v2_runtime_contract.py` с короткими проверками именно активного v2-верхнего слоя.
- Техническая проверка после правок:
  - новые/изменённые файлы компилируются;
  - полноценный запуск тестов в текущем окружении не был завершён, потому что отсутствуют зависимости `pytest`, `keyboard` и `pyautogui`.

### Этап 1. Процессы и аварийная остановка

- [x] Сверить `utils/process_control.py` с `PROCESS_MODEL.md`
- [x] Проверить поведение Esc
- [x] Проверить повторный запуск после остановки
- [x] Проверить, осталась ли где-то логика `stop_event` / мягкой остановки

Статус этапа: `Частично совпадает`

Что совпадает с планом:

- Активный запуск воркера идёт через `utils/process_control.py`.
- `start_worker()` не даёт запустить второй воркер, если текущий процесс ещё жив.
- `stop_worker()` делает жёсткую остановку через `terminate()` и затем `kill()` как fallback.
- `Esc` привязан к `ui/hotkeys.py` и вызывает `process_control.stop_worker()`.
- Воркер v2 запускается как отдельный процесс из `main.py` и `utils/generation_runner.py`.
- В актуальных браузерных v2-воркерах сигнатуры уже без `stop_event`: передаются `settings`, `coordinates`, `relative_movements`.

Что отличается от плана или требует оговорки:

- В `PROCESS_MODEL.md` центральным слоем назван `Process Manager`, но фактически активный контур уже смещён в `process_control.py` + `generation_runner.py`.
- В корне проекта всё ещё существует `utils/process_manager.py`, и он содержит старую промежуточную модель со `stop_event`.
- `utils/process_manager.py` не используется текущей точкой входа `main.py`, но импортируется в `tests/test_suite.py`, то есть legacy-слой ещё не изолирован полностью.
- В `ui/hotkeys.py` `Esc` сознательно не останавливает API-воркер: при активном API-режиме хоткей блокируется через `_block_if_api_running()`. Это расширение поведения, которого нет в базовой документации `PROCESS_MODEL.md`.

Следствие для дальнейшей проверки:

- Для браузерной ветки текущая модель процессов в целом соответствует замыслу v2: главный процесс + отдельный воркер + жёсткий stop по `Esc`.
- В репозитории остались следы старой логики остановки, но они находятся в legacy/transition-слое, а не в основном пути выполнения.
- При необходимости позже имеет смысл отдельно решить судьбу `utils/process_manager.py`: удалить, архивировать или явно пометить как legacy.

### Этап 2. Настройки и координаты

- [x] Сверить `utils/settings_store.py` с `SETTINGS_V2.md`
- [x] Сверить `utils/coordinates_store.py` с `COORDINATES_KEYS.md`
- [x] Проверить, действительно ли в режимы передаются два словаря отдельно

Статус этапа: `Частично совпадает`

Что совпадает с планом:

- `utils/settings_store.py` действительно является отдельным JSON-хранилищем без UI-логики.
- `load_settings()`, `save_settings()`, `apply_defaults()` реализованы и работают через `data/settings.json`.
- `CARDS_TO_PROCESS` пересчитывается автоматически из `START_FROM_CARD` и `END_CARD`.
- `SAVE_FOLDER`, `LOG_ENABLED`, `GENERATIONS_PER_CARD` в v2-хранилище не используются, что совпадает с документами.
- `utils/coordinates_store.py` хранит данные отдельно от `settings`.
- Формат `data/coordinates.json` соответствует документации: два ключа, `coordinates` и `relative_movements`.
- Дефолтные ключи координат совпадают с `COORDINATES_KEYS.md`, включая `PROMPT_INPUT_AFTER_IMAGE` и `TO_SAVE_OPTION`.
- Координаты дальше реально передаются в режимы двумя отдельными словарями, без склейки:
  - загрузка через `load_coordinates()`;
  - передача в `generation_runner`;
  - передача в `run_mode(tasks, settings, coordinates, relative_movements)`.

Что отличается от плана или требует оговорки:

- `settings_store.py` уже расширен API-настройками сильнее, чем это описано в базовой v2-спеке:
  - есть `API_MODEL_WITH_REFS`;
  - добавлены фактические значения по умолчанию для API-моделей и размеров.
- В `SETTINGS_V2.md` указано, что API-режим пока не поддерживает `multiformat_with_refs`, но код уже содержит:
  - `sites/aistudio/mode_multiformat_with_refs_api.py`
  - запуск этого режима из `generation_runner.py`
- В документации `BACK_ASPECT_RATIO` описан как опциональный для части сценариев, но в коде он всегда заполняется дефолтом (`16:9`).
- `settings_store.py` делает мягкое дополнение дефолтами, но не выполняет строгую валидацию типов и диапазонов на уровне хранилища.
- В `coordinates_store.py` при неожиданных значениях используется безопасный fallback `(0, 0)`, что практично, но не документировано как отдельное правило.

Вывод по этапу 2:

- Архитектурно слой настроек и координат соответствует замыслу v2.
- Модель с двумя словарями для координат подтверждена и реально используется в активном коде.
- Основная проблема здесь не в коде, а в том, что часть `SETTINGS_V2.md` уже отстаёт от фактической API-реализации.

### Этап 3. Консольное меню

- [x] Сверить `ui/console_menu.py` с `INTERFACE_console_menu.md`
- [x] Проверить показ плана генерации
- [x] Проверить выбор файла промптов и отображение текущей конфигурации

Статус этапа: `Частично совпадает`

Что совпадает с планом:

- Есть верхнеуровневые функции `show_main_menu`, `show_current_config`, `select_site`, `select_mode_for_site`, `select_prompts_file`, `show_generation_plan`, `start_generation_with_process`.
- `show_current_config()` выводит текущий сайт, режим и файл промптов.
- `select_site()` и `select_mode_for_site()` работают по нумерованным спискам.
- `select_prompts_file()` действительно сканирует папку `data/` и показывает только файлы.
- `show_generation_plan()` импортирует модуль выбранного режима, вызывает `load_tasks_from_file()` и `get_plan_info()`, затем печатает сводку.
- `start_generation_with_process()` не запускает UI-клики напрямую, а делегирует запуск воркера через `process_control.start_worker(...)`.
- Ссылка на текущий процесс для `Esc` хранится централизованно в `utils/process_control.py`, что по сути покрывает требование интерфейсного документа.

Что отличается от плана или требует оговорки:

- `show_main_menu()` при выборе `0` сохраняет настройки и выходит, но не останавливает активный подпроцесс явно. Это прямое расхождение с `INTERFACE_console_menu.md`, где перед выходом нужно останавливать воркер, если он запущен.
- Меню расширено новыми API-пунктами:
  - `6 — Выбрать метод генерации`
  - `7 — Настроить API ключ`
  Это не ошибка, но это уже выход за рамки базовой спецификации консольного меню.
- `show_current_config()` показывает больше полей, чем требует базовый контракт: метод генерации и статус API-ключа.
- `start_generation_with_process()` работает не с одним `coords`, а с двумя словарями: `coordinates` и `relative_movements`. Это соответствует фактической архитектуре v2, но не отражено в старом описании сигнатуры.
- `show_generation_plan()` ловит исключения слишком широко (`except Exception`), поэтому реальные ошибки чтения/парсинга маскируются общим сообщением.
- Сообщение после старта генерации сформулировано с оговоркой про режим запуска программы, а не просто как универсальное «Нажмите Esc для остановки».
- По замечанию пользователя, в реальном приложении сейчас нет рабочего/удобного консольного сценария для настройки файла промптов, и фактически путь до рабочего файла задаётся через ручное редактирование `settings.json`. Это нужно считать отдельной UX-проблемой, даже если базовая функция `select_prompts_file()` в коде присутствует.

Вывод по этапу 3:

- Базовый функционал консольного меню реализован и используется.
- Основное содержательное расхождение: выход из меню сейчас не гарантирует остановку уже запущенного воркера.
- Ещё одно практическое расхождение: настройка рабочего файла промптов через консоль по факту не является надёжным пользовательским сценарием, раз путь приходится задавать через `settings.json`.
- Документ `INTERFACE_console_menu.md` стоит считать частично устаревшим: фактическое меню уже включает API-ветку и реальную двухсловарную модель координат.

### Этап 4. Хоткеи

- [x] Сверить `ui/hotkeys.py` с `HOTKEYS_V2.md`
- [x] Отдельно проверить, что в v2 нет лишних hotkey-сценариев из v1

Статус этапа: `Частично совпадает`

Что совпадает с планом:

- Базовый набор хоткеев из `HOTKEYS_V2.md` реализован:
  - `Ctrl+Shift+P`
  - `Ctrl+0`
  - `Ctrl+Shift+V`
  - `Ctrl+Shift+S`
  - `Esc`
  - `Ctrl+1`
  - `Ctrl+3`
  - `Ctrl+4`
  - `Ctrl+5`
  - `Ctrl+6`
  - `Ctrl+7`
  - `Ctrl+8`
  - `Ctrl+9`
- `Ctrl+0` + `Ctrl+Shift+P` реализуют именно двухшаговую настройку координат, как и в документации.
- `Esc` привязан к жёсткой остановке браузерного воркера через `process_control.stop_worker()`.
- В v2 действительно отсутствуют старые хоткеи v1:
  - нет `Ctrl+2` для `GENERATIONS_PER_CARD`;
  - нет `Ctrl+Shift+Q` для остановки.

Что отличается от плана или требует оговорки:

- `Ctrl+7` в реальности делает больше, чем описано в `HOTKEYS_V2.md`:
  - выбирает метод генерации `browser/api`;
  - при выборе API настраивает модель, модель для референсов, размер и API-ключ;
  - затем даёт выбрать режим.
- Добавлен новый хоткей `Ctrl+Shift+A` для запуска API-воркера. В базовом `HOTKEYS_V2.md` его нет.
- Добавлен `Ctrl+Esc` для завершения консоли через `SIGINT`. В базовом документе он не описан.
- Во время активного API-воркера почти все хоткеи сознательно блокируются через `_block_if_api_running()`.
- `Esc` не останавливает API-воркер, если он сейчас запущен. Для браузерной ветки это нормально, но в общем описании `HOTKEYS_V2.md` такое исключение не зафиксировано.
- Меню координат и часть сообщений отличаются по формулировкам от примеров в документации, хотя сценарий в целом тот же.

Вывод по этапу 4:

- Базовый v2-набор хоткеев реализован и старые v1-сценарии в основном убраны.
- `HOTKEYS_V2.md` уже частично устарел: фактические хоткеи включают API-ветку и дополнительный сервисный хоткей `Ctrl+Esc`.
- Нужно отдельно решить, считать ли блокировку `Esc` для API-воркера корректным новым правилом и задокументировать это.

### Этап 5. Window manager

- [x] Сверить `utils/window_manager.py` с документами и ожиданиями v2

Статус этапа: `Частично совпадает`

Что совпадает с планом:

- Есть отдельный модуль `utils/window_manager.py`.
- Реализованы два основных сценария, ожидаемые документами:
  - `quick_setup_window()` для `Ctrl+Shift+V`;
  - `setup_automation_window()` для автоподготовки перед запуском генерации.
- Настройка окна соответствует общему замыслу:
  - поиск окна браузера;
  - активация;
  - установка размера `1200x1000`;
  - перемещение в `(0, 0)`.
- Если существующее окно не найдено, `setup_automation_window()` умеет перейти к созданию нового окна браузера, что соответствует сценарию из `SETTINGS_V2.md`.

Что отличается от плана или требует оговорки:

- `quick_setup_window()` только ищет и настраивает существующее окно; если окно не найдено, он не создаёт новое. Это в целом совпадает с `COORDINATES_SETUP_GUIDE.md`, но важно как фактическое ограничение UX.
- Поиск окна браузера построен на очень широком совпадении по заголовкам (`Chrome`, `Firefox`, `Edge`), без проверки, что это именно окно AI Studio.
- `create_automation_window()` открывает `about:blank`, а не AI Studio; дальше окно просто используется как рабочее браузерное окно.
- Проверка успешного создания окна сделана косвенно через движение курсора в `_verify_window_created()`, а не через надёжную валидацию конкретного окна.
- В модуле есть поле `window_title = "AI Studio Automation Window"`, но по факту оно нигде не используется.
- В логике модуля нет явной привязки к `GENERATION_METHOD`: даже при API-режиме сам модуль существует и может вызываться, хотя по спецификации API-ветке окно браузера не требуется.

Вывод по этапу 5:

- По назначению `window_manager` соответствует v2 и покрывает оба основных сценария: быстрая настройка и автоподготовка.
- Реализация более практическая, чем формальная спецификация, но местами грубая: особенно в поиске нужного окна и проверке успешного создания.
- Документация про окно в целом актуальна по смыслу, но не отражает некоторых технических деталей и ограничений текущей реализации.

### Этап 6. Общие helper-функции AI Studio

- [x] Сверить `sites/aistudio/helpers.py` с `INTERFACE_aistudio_helpers.md`
- [x] Проверить ожидание готовности изображения
- [x] Проверить сохранение изображений и работу с буфером

Статус этапа: `Частично совпадает`

Что совпадает с планом:

- `sites/aistudio/helpers.py` действительно является общим слоем UI-автоматизации для режимов.
- Низкоуровневые функции есть:
  - `move_and_click`
  - `press_keys`
- Основные helper-функции присутствуют:
  - `click_prompt_input`
  - `paste_prompt_text`
  - `click_new_chat`
  - `rename_chat`
  - `select_aspect_ratio`
  - `start_generation`
  - `save_image`
- Реализован отдельный блок проверки готовности изображения:
  - `grab_result_area`
  - `compute_difference_score`
  - `wait_until_image_ready`
- В режимах `standard`, `multiformat`, `multiformat_with_refs` UI-действия вызываются через `helpers`, без прямых вызовов `pyautogui` внутри самих режимов.
- `save_image()` использует `IMAGE_LOCATION` и `TO_SAVE_OPTION`, как и ожидается по документации.

Что отличается от плана или требует оговорки:

- В `helpers.py` есть `type_text()` и он используется в `select_aspect_ratio()`. Это не ошибка, а практическое исключение из общего правила “ввод через буфер обмена и Ctrl+V”: для поля выбора `aspect ratio` в текущем UI прямой ввод через `Ctrl+V` не работает, поэтому посимвольный ввод здесь оправдан.
- Для обычного текста промпта используется `pyperclip.copy(...)`, а не отдельный проектный слой `utils.clipboard`. По смыслу это работает, но архитектурно расходится с идеей централизованного буфера обмена.
- `rename_chat()` реализован как:
  - клик по `CHAT_NAME_INPUT`
  - `Ctrl+A`
  - вставка имени
  - `Enter`
  При этом `CHAT_NAME_POPUP` и `CHAT_NAME_CONFIRM` в helper-функции не используются, хотя документация оставляет их как возможный путь.
- `save_image()` не восстанавливает буфер обмена после сохранения имени файла, хотя в старых алгоритмах это упоминалось как желательное поведение.
- В `copy_image_to_clipboard()` логика Windows-буфера есть, но она живёт в `helpers.py`, а не в отдельном специализированном модуле.
- `wait_until_image_ready()` возвращает `True/False`, но вызывающая логика standard-режима не логирует отдельно факт таймаута против успешного раннего срабатывания.

Проверка ожидания готовности изображения:

- Алгоритм с baseline-скриншотом, периодическими проверками и порогом `diff_threshold` реализован и соответствует `IMAGE_READY_DETECTION.md`.
- Нормализация `compute_difference_score()` выполнена через `PIL.ImageChops` и `ImageStat`, что согласуется с допускаемой реализацией из документа.
- Используются фактические настройки:
  - `GENERATION_WAIT`
  - `IMAGE_WAIT_INTERVAL`
  - `IMAGE_CHECK_BOX_SIZE` / fallback `(100, 100)`
  - `IMAGE_CHECK_THRESHOLD` / fallback `0.1`

Вывод по этапу 6:

- Helper-слой v2 по сути реализован и используется правильно как единая прослойка между режимами и `pyautogui`.
- Главные расхождения: частичный отход от правила “только clipboard + Ctrl+V”, а также некоторые практические детали вокруг буфера обмена и логирования результата ожидания.

### Этап 7. Режим `standard`

- [x] Сверить `load_tasks_from_file`
- [x] Сверить `get_plan_info`
- [x] Сверить `run_mode`
- [x] Проверить соответствие `PROMPTS_standard_format.md`, `NAMING_RULES.md`, `LOGGING.md`

Статус этапа: `Частично совпадает`

Что совпадает с планом:

- `load_tasks_from_file()` использует regex `^Карточка (\\d+) - Промпт (\\d+): (.+)$`, что соответствует `PROMPTS_standard_format.md`.
- Пустые строки игнорируются, дубликаты промптов перезаписываются последним значением, итоговый порядок сортируется по карточке и номеру промпта.
- `get_plan_info()` корректно считает:
  - `cards_count`
  - `generations_count`
  - `images_planned`
- Именование совпадает с `NAMING_RULES.md`:
  - чат: `Карточка {N} - генерация {G}`
  - файл: `Карточка_{N}_лицевая_промпт_{G}.png`
- `run_mode()` работает в ожидаемой архитектуре:
  - фильтрация по диапазону карточек;
  - проверка обязательных координат;
  - лог `[PLAN]`;
  - цикл по задачам;
  - создание нового чата;
  - вставка промпта;
  - переименование;
  - запуск генерации;
  - ожидание;
  - сохранение файла;
  - итоговый `[SUMMARY]`.
- В режиме нет прямых вызовов `pyautogui`, только вызовы через `helpers`.

Что отличается от плана или требует оговорки:

- Невалидные строки при парсинге просто пропускаются без предупреждения, хотя `PROMPTS_standard_format.md` ожидает предупреждение по нераспознанным строкам.
- `load_tasks_from_file()` возвращает уже плоский список задач, а не более богатую промежуточную структуру из примеров документации. Это не ошибка, но это фактический контракт реализации.
- В `run_mode()` при отсутствии обязательных координат ошибка выводится только в консоль, но не логируется в файл как `[ERROR]`.
- Прогресс в консоль печатается как `Генерация X из Y`, где `X` — число успешно сохранённых файлов, а не номер текущей попытки. Это может вводить в заблуждение при ошибках и пропусках.
- После завершения последнего промпта карточки вызывается `update_start_card(card_number + 1)`. Это важная фактическая логика продолжения с места остановки, которой нет в исходной спецификации standard-режима.
- При `CHECK_IMAGE_GENERATED = True` результат `wait_until_image_ready()` не анализируется содержательно:
  - при `False` режим всё равно идёт дальше к сохранению;
  - отдельный `[WARN]` о таймауте ожидания не пишется.
- Лог-файл создаётся на запуск `run_mode()`, а не на запуск всей программы в целом. Это расходится с формулировкой из `LOGGING.md`, где файл описан как “один на запуск программы”.
- Внутри `run_mode()` лог-файл открывается напрямую через `open(...)`, а не через общий logging-слой. По сути это рабочее решение, но менее системное, чем описано в `LOGGING.md`.

Вывод по этапу 7:

- Standard-режим реализован и в своей основе хорошо совпадает с документами v2: формат промптов, порядок задач, naming и общий цикл генерации соответствуют плану.
- Основные расхождения не ломают архитектуру, но важны эксплуатационно:
  - нет предупреждений по невалидным строкам;
  - таймаут ожидания изображения отдельно не фиксируется;
  - есть недокументированная логика авто-сдвига `START_FROM_CARD`;
  - логирование устроено проще и локальнее, чем заявлено в `LOGGING.md`.

### Этап 8. Режим `multiformat`

- [x] Сверить парсинг пар лицо/оборот
- [x] Сверить `FACE_ASPECT_RATIO` / `BACK_ASPECT_RATIO`
- [x] Сверить порядок выполнения шагов и именование файлов

Статус этапа: `Частично совпадает`

Что совпадает с планом:

- `mode_multiformat.py` реализует отдельный режим с лицом и оборотом в разных чатах.
- Именование совпадает с `NAMING_RULES.md`:
  - чат: `Карточка {N} - {Name} - {Side} - Промпт {P}`
  - файл: `Карточка_{N}_{Side}_промпт_{P}.png`
- В `run_mode()` используются `FACE_ASPECT_RATIO` и `BACK_ASPECT_RATIO`.
- Порядок задач в `load_tasks_from_file()` соответствует ожидаемому:
  - по карточкам;
  - по парам;
  - внутри пары сначала `лицо`, затем `оборот`.
- Для каждой стороны выполняется ожидаемый цикл:
  - новый чат;
  - ввод промпта;
  - переименование;
  - выбор aspect ratio;
  - запуск;
  - ожидание;
  - сохранение.
- Проверка обязательных координат включает `ASPECT_RATIO_SELECTOR`, как и требуется для режима.
- Логирование по смыслу покрывает основные теги режима: `[PLAN]`, `[CARD]`, `[PAIR]`, `[SIDE]`, `[GEN]`, `[OK]`, `[WARN]`, `[SUMMARY]`.

Что отличается от плана или требует оговорки:

- Регулярное выражение парсера в коде шире, чем в `PROMPTS_multiformat_format.md`:
  - документ предполагает название без дефисов (`[^-]+`);
  - код использует `(.+?)`, то есть поддерживает названия с дефисами.
  Это скорее полезное расширение, чем ошибка.
- При несовпадении названий карточки в разных строках код молча использует первое имя и не пишет предупреждение, хотя документ ожидает предупреждение.
- Невалидные строки в файле промптов пропускаются без предупреждений.
- Неполные пары в реализации обрабатываются иначе, чем в ряде текстовых описаний:
  - внутренняя структура `_parse_file_to_cards()` действительно хранит `None` для отсутствующей стороны;
  - но `load_tasks_from_file()` не создаёт задачи для отсутствующей стороны вовсе.
  В итоге в плоском списке задач остаются только реально существующие стороны.
- Как и в standard-режиме, результат `wait_until_image_ready()` по сути не анализируется: при таймауте код всё равно идёт к сохранению без отдельного `[WARN]`.
- После завершения последнего промпта карточки вызывается `update_start_card(card_number + 1)`, но это поведение отдельно не описано в спецификации multiformat.
- Лог-файл создаётся на запуск `run_mode()`, а не как единый файл на весь запуск программы.

Вывод по этапу 8:

- Multiformat-режим реализован и по основному пользовательскому поведению хорошо совпадает с планом.
- Главные расхождения касаются парсинга и логирования:
  - код поддерживает более широкий формат названий;
  - предупреждения по проблемным строкам и конфликтам имён почти не ведутся;
  - таймаут ожидания генерации отдельно не фиксируется.

### Этап 9. Режим `multiformat_with_refs`

- [x] Сверить логику поиска референсов
- [x] Сверить шаг вставки референса перед промптом
- [x] Проверить дополнительные координаты и зависимости

Статус этапа: `Частично совпадает`

Что совпадает с планом:

- Режим построен как расширение `multiformat`.
- Используется дополнительная обязательная координата `PROMPT_INPUT_AFTER_IMAGE`.
- Реализован поиск референса по двум форматам имени файла с правильным приоритетом:
  1. `{card_number}_{side}.{ext}`
  2. `{side}_{card_number}_{safe_card_name}.{ext}`
- Поддерживаются ожидаемые расширения `png` и `jpg`.
- Вставка референса выполняется перед вводом промпта:
  - поиск файла;
  - копирование изображения в буфер;
  - `Ctrl+V`;
  - пауза `AFTER_IMAGE_PASTE`;
  - клик в `PROMPT_INPUT_AFTER_IMAGE`;
  - вставка промпта.
- Именование совпадает с `NAMING_RULES.md`:
  - чат как в `multiformat`;
  - файл: `Карточка_{N}_{safe_name}_{side}_промпт_{P}.png`.
- Дополнительная предварительная проверка референсов есть через `check_all_references(tasks)`.

Что отличается от плана или требует оговорки:

- В `ALGO_multiformat_with_refs.md` референсы описаны как обязательные, но фактическая реализация не блокирует запуск при их отсутствии.
- Документ предполагает сценарий:
  - показать отсутствующие референсы;
  - спросить `Продолжить без них? (y/n)`;
  - при отказе отменить запуск.
  В коде этого нет: режим всегда продолжает работу без найденных референсов.
- При отсутствии референса сторона генерируется как обычный `multiformat`, с `[WARN]`, но без отдельного пользовательского решения.
- `check_all_references()` считает проверки по задачам, то есть по сторонам, а не по уникальным парам/карточкам. Это нормально практически, но отличается от некоторых текстовых формулировок документации.
- Как и в других режимах, результат `wait_until_image_ready()` не анализируется содержательно: отдельный `[WARN]` по таймауту не пишется.
- После завершения карточки снова вызывается `update_start_card(card_number + 1)`, хотя это не зафиксировано в исходной спецификации.
- В консоли при отсутствующих референсах выводится только информационное сообщение о продолжении, без диалога подтверждения.

Уточнение от пользователя:

- Для реального рабочего процесса это текущее поведение считается правильным: часть карточек пользователь делает с референсами, а часть без них.
- Значит, генерация без референса в `multiformat_with_refs` — не проблема, а полезный поддерживаемый сценарий.
- Следовательно, здесь нужно скорее обновить документацию под фактическое поведение, чем считать это багом режима.

Вывод по этапу 9:

- Режим `multiformat_with_refs` реализован и основная механика вставки референса работает в соответствии с замыслом v2.
- Главный содержательный разрыв с документацией: референсы фактически стали необязательными для запуска, а отсутствие файлов переводится в режим “продолжить без них”.
- По уточнению пользователя это поведение нужно считать корректным и полезным для смешанного рабочего процесса.
- Значит, задача здесь не в изменении кода, а в приведении документации к реальному и желаемому поведению режима.

### Этап 10. Полировка и эксплуатационные детали

- [x] Сверить логирование с `LOGGING.md`
- [x] Сверить пользовательский сценарий с `USER_FLOW.md`
- [x] Сверить проблемные места с `TROUBLESHOOTING.md`
- [x] Собрать фактические замечания по текущей работе программы

Статус этапа: `Частично совпадает`

Что совпадает с планом:

- Файловое логирование в целом существует:
  - есть `utils/log_writer.py`;
  - режимы пишут строки с timestamp и тегами `[PLAN]`, `[CARD]`, `[PAIR]`, `[SIDE]`, `[GEN]`, `[OK]`, `[WARN]`, `[ERROR]`, `[SUMMARY]`.
- Логи действительно пишутся в папку `logs/` с именем `auto-gen_YYYY-MM-DD_HH-MM-SS.log`.
- В консоль действительно выводится упрощённая информация:
  - старт;
  - прогресс;
  - завершение;
  - сообщения об остановке.
- В проекте есть пользовательские документы по сценарию использования и troubleshooting, то есть эксплуатационный слой задуман и частично поддерживается.

Что отличается от плана или требует оговорки:

- `LOGGING.md` описывает один лог-файл на запуск программы, но фактически браузерные режимы создают свой лог-файл внутри каждого `run_mode()`.
- В проекте остался старый `utils/logger.py`, который выводит сообщения в консоль и не соответствует фактической v2-модели логирования. Это legacy-слой, способный путать картину.
- `USER_FLOW.md` существенно устарел относительно реального приложения:
  - описывает меню с пунктами `1..8`, которого сейчас нет в таком виде;
  - ориентируется на консольный сценарий как основной;
  - не отражает фактическую hotkey-first модель из `main.py` + `ui/console.py`;
  - не отражает API-ветку в её текущем виде.
- `TROUBLESHOOTING.md` тоже частично устарел:
  - описывает `Esc` как универсальную остановку, но в реальности API-воркер `Esc` не останавливает;
  - не фиксирует проблему, что рабочий файл промптов пользователю приходится задавать через `settings.json`;
  - не фиксирует, что отсутствующие референсы в `multiformat_with_refs` сейчас не блокируют запуск;
  - не фиксирует, что в API-режиме хоткеи в фоне блокируются специально.
- `ui/console.py` содержит сильно устаревший legacy-код рядом с актуальной инструкцией `show_instructions()`:
  - импортирует `config.coordinates`;
  - содержит старую модель `show_current_settings(...)`;
  - смешивает v1/v2-подходы.
  Это ещё один индикатор того, что пользовательский слой приложения не дочищен.

Фактические замечания по текущей работе программы, собранные в ходе проверки:

- В реальном пользовательском сценарии нет надёжной рабочей настройки файла промптов через консоль; путь приходится задавать через `settings.json`.
- Выход из `show_main_menu()` не гарантирует остановку уже запущенного воркера.
- `Esc` не останавливает API-воркер, хотя в документах это не проговорено как исключение.
- Отсутствующие референсы в `multiformat_with_refs` больше не являются жёстким блокером запуска, хотя документация по-прежнему описывает их как обязательные.
- Результат `wait_until_image_ready()` в режимах фактически не влияет на дальнейшее поведение: по таймауту генерация всё равно идёт к сохранению.
- Почти во всех режимах невалидные строки входных файлов и конфликты имён карточек не дают явных предупреждений, хотя документы это ожидают.
- Логика авто-сдвига `START_FROM_CARD` после завершения карточки работает, но документально почти не описана.
- Пользователь отдельно отметил желаемый формат прогресса в консоли: `Генерация X/Y из Z`, где `X` — количество успешных генераций, `Y` — количество уже выполненных попыток генерации, `Z` — общее запланированное количество генераций. Текущий вывод вида `Генерация X из Z`, где `X` — только число успехов, воспринимается как вводящий в заблуждение.
- Пользовательская документация (`USER_FLOW`, `TROUBLESHOOTING`) заметно отстаёт от реального продукта и уже не может считаться точным руководством.

Вывод по этапу 10:

- Технически эксплуатационный слой в приложении есть, но он уже расходится с текущим фактическим UX и архитектурой.
- Главная проблема этапа 10 — не отсутствие функциональности, а рассинхрон между кодом, пользовательскими документами и реальным способом работы с программой.
- Для дальнейшего развития приложения имеет смысл завести отдельный список приоритетных UX-долгов поверх архитектурной сверки.

## Отдельный блок: что нужно уточнить у пользователя

- [ ] Спросить, какие именно проблемы и шероховатости есть в текущей работе программы
- [ ] Спросить, какие из этих проблем самые критичные
- [ ] Спросить, что считается “полировкой”, а что уже требует изменения логики/архитектуры
- [ ] После опроса добавить отдельный раздел с приоритетами доработок

## Формат дальнейшей работы по этому файлу

Предлагаемый статус для каждого блока:

- `Не проверено`
- `Совпадает с планом`
- `Частично совпадает`
- `Отличается от плана`
- `Нужно решение/уточнение`

## Ближайший следующий шаг

Следующим шагом пройтись по коду по крупным блокам:

1. `main.py` + `utils/process_control.py` + `ui/hotkeys.py`
2. `ui/console_menu.py`
3. `utils/settings_store.py` + `utils/coordinates_store.py`
4. `sites/aistudio/helpers.py`
5. `sites/aistudio/mode_standard.py`
6. `sites/aistudio/mode_multiformat.py`
7. `sites/aistudio/mode_multiformat_with_refs.py`
